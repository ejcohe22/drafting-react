import { Box, useTheme } from "@mui/material";
import { tokens } from "../../theme";

const Refresh_Graph = () => {
  const theme = useTheme();

  return (
    <Box m="20px" />
  )
};

export default Refresh_Graph;